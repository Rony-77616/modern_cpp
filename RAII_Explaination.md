# 一.C编程的缺点
## C编程的一个主要类型的BUG就是内存泄露，通常是由于new完一块对内存后，忘记delete或者free而导致的内存泄露。


# 二.RAII是什么

## 1.RAII就是“资源获取即初始化”

## 2.其理念就是，资源（堆内存等）应当归对象所有。 该对象在创建时，就利用构造函数创建或接收新分配的资源，并且在析构函数中将此资源删除释放。RAII原则可以确保当对象超出生命范围的时候，其所有的资源都可以正确返回到操作系统。


# 三.智能指针

## 1.智能指针的用途？
### 为了应用RAII原则，智能指针可以处理其拥有的内存的分配和删除。

## 2.什么是智能指针？
### 智能指针是指向动态分配（即堆）内存的指针的类，在指针离开作用域时，可以自动回收该内存，防止内存泄露。智能指针的核心是引用计数，每使用一次，指针内部引用计数+1，每析构一次，内部引用计数-1，直到为0，删除所指向的堆内存。



## 3.智能指针的分类

### 智能指针主要分为三大类:

#### A.shared_ptr：共享的智能指针。特点是多个shared_ptr可以同时指向同一块有效的内存。有三种方法来初始化一个shared_ptr：通过构造函数初始化，通过std：make_shared辅助函数，以及reset初始化的方法。共享智能指针对象初始化完成后就指向了要管理的那块内存，可以用use_count成员函数来查看当前有多少个智能指针管理着这块内存。

##### 初始化：构造函数初始化，移动构造初始化，拷贝构造初始化，reset初始化。

##### 使用：get方法取出原始地址，或者直接当作一个指针来使用

##### 指定删除器: 当智能指针管理一个地址连续存储的数组时，直接用默认的删除器来释放内存是不行的，他只会释放一个元素的内存地址，因此需要指定删除器，可以在外面写也可以用匿名函数，也就是一个lambda表达式来指定。如果管理数组时，不想自己手动写删除器，也可以用c++自带的默认数组删除器来释放内存。




#### B.unique_ptr: 独占的智能指针。特点是只能由一个unique_ptr单独指向一块有效的内存，不能有其它智能指针同时指向这块内存。有三种方法来初始化一个unique_ptr：通过构造函数初始化，通过移动构造初始化，以及reset函数初始化。

##### 初始化：构造函数初始化，移动构造初始化，reset方法初始化。

##### 使用：get方法获取原始地址，或者直接当作一个指针来使用

##### 指定删除器：独占的智能指针的删除器函数与shared_ptr有不同，shared_ptr的删除器不需要指定删除器类型，也就是不需要写删除器类型的参数，而unique_ptr的删除器需要指定删除器的类型。unique_ptr指定删除器类型是通过第二个模板参数指定的。假如删除器是一个普通函数，函数本身不是类型，而函数指针才是类型。假如lambda表达式是删除器类型，且是无捕获的，可以隐式转换为函数指针，用一个函数指针来表示类型，若是有捕获的，也可以用decltype。管理数组的时候，可以不用删除器函数，在模板参数那里指定是一个数组类型，其实shared_ptr也可以用这个方法，只是c++11以后才支持。





#### weak_ptr:
